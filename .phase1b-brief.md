# Phase 1b — Session cards + nav fix + ladder tuning (ticket tk_a49af2b3fa3a4020, same branch)

You are still Prism. Same rules as before: branch `feat/dashboard-redesign` only, never push, sessionfsbot author, no AI mentions in commits.

CEO reviewed the Phase 1 screenshots. Foundation approved as plumbing, but two fixes are needed and the first VISIBLE chunk is now in scope: the session list cards. Goal: this is where "mission control for AI sessions" becomes visible — tool-coded, dense, machined.

## Fix 1 — Nav active pill artifact (dashboard/src/components/Layout.tsx)

The `border-l-2` brand left-edge on the active nav pill renders like a stray "(" bracket on a horizontal top nav (it was specced for a sidebar — wrong context). Remove the left-edge approach entirely. Active state = `--surface` pill + `text-primary` + a subtle full border (`border border-[var(--border)]`). Inactive unchanged (transparent, hover `--surface-hover`). Apply to both the main nav links AND the Admin link.

## Fix 2 — Widen the dark ladder (dashboard/src/index.css)

Cards still sit too close to the canvas in dark. Adjust ONLY the dark theme:
- `--surface`: 21% → 22%
- `--surface-hover`: 23% → 24.5%
- `--surface-active`: stays 26%
- `--bg-elevated`: 24% → 26%
- `--overlay`: 27% → 29%
Keep chroma/hue as-is. Light theme untouched.

## The chunk — session cards (dashboard/src/sessions/SessionList.tsx)

Restyle the session list items and the hero "current session" card. Use `TOOL_COLORS` from `src/utils/tools.ts` — never hardcode hex.

Per-session card:
1. **3px tool-colored left edge** — the signature element. Implemented as a left border or inset box-shadow in the tool's color (`TOOL_COLORS[source_tool]`, fallback `var(--text-tertiary)`), respecting the card radius.
2. **Hierarchy:** title 15px/semibold/`text-primary` (one line, truncate). Metadata row below: tool name + short session id rendered as `.text-mono-chip` elements; message count / token count / model as 13px `text-tertiary` separated by middots. Kill any visual noise that competes with the title.
3. **Hover:** background `--bg-elevated`, border `--border-strong`, `transition: 150ms ease` on background/border. No translate/scale (keep it machined, not bouncy).
4. **Density:** card padding ~14px vertical. Cards on `--surface` with `--border` hairline, `--radius-lg`.
5. Keep ALL existing behavior: links/click targets, bookmark button + its pop animation, multi-select checkboxes, relative time on the right, group headers (THIS WEEK etc. — restyle as 11px uppercase `text-micro` `text-tertiary` with more top spacing), pagination, empty states.

Hero "current session" card at top:
- Surface `--bg-elevated`, hairline border, the same tool-colored left edge but 4px.
- "Resume in <tool>" primary button gets `box-shadow: 0 0 0 3px var(--brand-glow)` on focus AND a subtle resting glow (`0 4px 16px var(--brand-glow)`).
- Title can go 17px/semibold. Keep all existing buttons/links/behavior.

## Verification
1. `npx tsc -b` clean.
2. `npm test` — all 209 pass. SessionList tests assert behavior; if one asserts a removed classname, update it minimally and note it.
3. No hardcoded hex added anywhere (use tokens / TOOL_COLORS).
4. Commit as one or two logical commits. Print a summary with files changed, test results, and any test updates.

## Out of scope
Everything else: SessionDetail, other pages, primitives, motion beyond the specified hover/glow, landing site.
