# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

SessionFS — Portable session layer for AI coding tools.

## Agent Team

Project-specific agent personas are in `.agents/`. Load the relevant agent for your task before starting work. See `.agents/README.md` for the full assignment matrix.

| Agent | File | Domain |
|-------|------|--------|
| Atlas | `.agents/atlas-backend.md` | Backend, daemon, API, CLI |
| Sentinel | `.agents/sentinel-security.md` | Security, auth, audit |
| Bastion | `.agents/bastion-security-auditor.md` | Security posture audit, adversarial verification, service-key route tests |
| Forge | `.agents/forge-devops.md` | CI/CD, Docker, distribution |
| Prism | `.agents/prism-frontend.md` | Web dashboard, VS Code extension |
| Scribe | `.agents/scribe-docs.md` | Documentation |
| Reach | `.agents/reach-go-to-market.md` | Sales execution, outbound, demos, prospect research |
| Harbor | `.agents/harbor-customer-success.md` | Customer-facing support execution, onboarding, QBRs |
| Compass | `.agents/compass-product.md` | Product roadmap, feature specs, prioritization |
| Onyx | `.agents/onyx-incident-commander.md` | Incident command, status comms, post-mortems |
| Ledger | `.agents/ledger-revenue.md` | Revenue, billing, Stripe, pricing |
| Shield | `.agents/shield-compliance.md` | Compliance, data governance, certifications |
| Shield-SR | `.agents/shield-security-review.md` | Pre-release security review (MANDATORY before every release) |
| Scribe-Site | `.agents/scribe-site-sync.md` | Pre-release site sync (MANDATORY — no stale site content) |
| Vault | `.agents/vault-licensing.md` | Licensing, open source, IP protection |
| Beacon | `.agents/beacon.md` | BizDev, partnerships, platform-integration discovery |

## Architecture

- **Daemon (sfsd):** Background process using fsevents/inotify (not polling) to watch native AI tool session storage (Claude Code, Codex, Gemini CLI, Cursor) and capture sessions into canonical `.sfs` format
- **CLI (sfs):** Command-line tool for browsing, pulling, resuming, forking, and handing off sessions
- **API Server:** FastAPI + PostgreSQL + S3/GCS for cloud session storage and team features
- **Web Dashboard:** React management interface (NOT a chat UI — users interact with their native AI tools)

### Session Format (.sfs)

A `.sfs` session is a directory containing: `manifest.json`, `messages.jsonl`, `workspace.json`, `tools.json`. All file paths within are relative to workspace root. Sessions are append-only — conflict resolution appends both sides rather than merging.

## Commit Rules

- **All commits must use author `sessionfsbot <bot@sessionfs.dev>`.** Use `--author="sessionfsbot <bot@sessionfs.dev>"` on every git commit.
- **NEVER include "Co-Authored-By" lines referencing Claude, Anthropic, or any AI assistant.**
- **NEVER mention Claude Code, Claude, AI, LLM, or any AI tooling in commit messages.**
- Commit messages should read as if written by a human developer. Focus on what changed and why.

## Git Branch Policy

- **`develop` is LOCAL ONLY.** NEVER push develop to origin. The public repo must only have `main`. Develop contains internal files (.agents/, src/spikes/, docs/security/, DOGFOOD.md, brand/, .release/, CLAUDE.md) that must never be public.
- **All work happens on `develop` locally.** Commits go here. Tests run here.
- **Public releases: merge to `main`, sanitize, push.** Use `.release/private-files.txt` to strip internal files before pushing main. The `/release` skill handles this.
- **NEVER run `git push origin develop`.** This is a security breach — it exposes internal strategy, agent personas, threat models, and business docs.
- **If develop is accidentally pushed:** delete immediately with `git push origin --delete develop`.

## Multi-LLM Review

Before major releases, use a second LLM (e.g., Gemini CLI) to review specific areas. This catches blind spots and provides architectural critique.

```bash
export GOOGLE_GEMINI_BASE_URL="http://100.96.105.123:4000"
export GEMINI_API_KEY=sk-training-2025
gemini --model gemini-3-flash-preview --yolo -p "Review [area] in the codebase and suggest improvements..."
```

**When to use:** UI/UX review, architecture critique, missed edge cases, security audit of new features.
**Not for:** Writing code. Use it for reviewing what was built, not generating implementations.
**Models available:** gemini-3-flash-preview, gpt-5, claude-opus-4.1, deepseek-r1 (via proxy at 100.96.105.123:4000)

## Release Process

- **ALWAYS use the `/release` skill** (at `.claude/commands/release.md`) for every release. No ad-hoc releases.
- The skill covers: version bump, changelog, docs update, ruff lint, tests, deploy landing + dashboard, merge to main with sanitization, tag, verify pipelines, update memories.
- **Run `ruff check src/` before committing.** Lint failures break CI and are embarrassing.
- **Run tests before every release.** No exceptions.

## Key Decisions (Do Not Violate)

- NO WebSockets, NO Redis, NO real-time sync. HTTP + ETags only.
- NO server-side LLM API keys. All LLM calls are client-side.
- Daemon defaults to local-only. Cloud sync is explicit opt-in.
- All file paths in .sfs format are relative to workspace root.
- Sessions are append-only. Never modify messages in place.
- Team handoff is the core monetization wedge. Individual tier is free forever.
- **All GCP infrastructure must be created via Terraform.** No `gcloud` imperative commands for resource creation. Secrets stored in GCP Secret Manager only — never in code, env files, or CI configs.

## Current Phase

**v0.11.1 shipped 2026-06-20 — patch: hardened the v0.11.0 entitlements backfill migration + restored green CI**

Same-day fast-follow. Migration 050's backfill emitted a diagnostic via `op.execute(sa.text(...), {params})` — but alembic's `op.execute` takes only `execution_options`, not a positional bind-param dict, so on any upgrade where `DIAGNOSTIC_ENTRIES` was non-empty (a tier coercion or unmatched license) the migration would raise an unbound-parameter error. v0.11.0's prod migrate-job happened to succeed (prod data was clean → diagnostic branch skipped), but mypy flagged it (`Too many positional arguments for "execute"`) and turned the v0.11.0 CI run RED — the local /release flow runs ruff + tests but NOT mypy, so it slipped through. Fix: removed the discarded `SELECT` (it produced no durable record anyway) and emit diagnostics to the migration log via `print`. **Process fix: the /release skill now runs `mypy src/sessionfs/ --ignore-missing-imports` in step 3** (the CI `lint` job's exact invocation) so type errors are caught before tagging. No schema change; migrations 001–052 unchanged; already-migrated DBs unaffected. 2415 backend + 388 dashboard tests.

**v0.11.0 shipped 2026-06-20 — licensing + org-management redesign (entitlements as single source of truth; enterprise admins can manage their orgs)**

2415 backend tests (+136) + 388 dashboard UI tests (unchanged). DB migrations: 001–**052** (050 additive entitlements foundation; 051 `org_owner_transfer`; 052 role/status CHECK constraints). 62 MCP tools (unchanged). Backend + dashboard (no new CLI commands, no new MCP tools). The licensing redesign replaces scattered plan/seat/storage state (across user rows + license tables) with a single authoritative **`entitlements`** record per org — `status` (active|canceled|expired|revoked) tracked SEPARATELY from `billing_status` (current|past_due); partial unique `uq_entitlements_one_active_per_owner`; deterministic backfill (license→subscription→manual). Shipped across 4 phases each via DeepSeek worker → verification → Codex + Sentinel + Shield review → merge: **P1** (c3f9be0) entitlements model + migration 050 + `OrgRole.OWNER` + ActivationAttempt/PendingLicenseClaim/OrgAuditEvent tables + HelmLicense.org_id; **P2** (5ca1302) NEW `services/entitlements.py` (`resolve_entitlement` read, owner-derived server-side; `apply_entitlement` write-through + legacy-cache mirror; rejects tier='admin') + `tier_gate.get_effective_tier` rewired entitlement-first with legacy fallback + routes/auth.py /me enrichment (effective_tier/org_id/org_name/org_role) + dashboard Layout.tsx org gate; **P3** (20dae1a) NEW `routes/activation.py` (GET /org/activate/info non-oracular + POST /org/activate Phase A durable attempt+emailed single-use code + POST /org/activate/verify Phase B atomic; token sha256-hashed, raw never persisted; edge rate-limited via Cloud Armor) + admin retention sweeper; **P4** (8477094) migration 051 `org_owner_transfer` (partial-unique one-pending) + migration 052 role/status CHECKs + org_members.py owner guards + initiate/accept/cancel/get transfer endpoints (`_lazy_expire_transfer`) + admin.py last-owner deactivation safety + force-transfer. Review fixes caught real bugs each phase (P1 SQLite inline CHECK + single-active backfill; P2 quota-zeroing on tier-only mirror + Stripe-provenance snapshot; P3 emailed-code-vs-hash truncation bug caught by Sentinel+Shield + slug-collision auto-suffix; P4 lazy-expire frees one-pending slot + FOR UPDATE auto-promote). Shield-SR v0.11.0 pre-release CLEAN (0 critical/high; pydantic-settings>=2.14.2 defense-in-depth bump; 1 non-blocking LOW filed: /verify existing-member re-check + /me `.first()`). **CLOUD ARMOR LIVE in prod** (tk_c279911c20264333 — the P3-activation prod gate): api.sessionfs.dev → global ALB → Cloud Armor (per-IP + per-path deny-429 on activate/helm-validate) → Cloud Run, TLS via Certificate Manager DNS-auth cert; zero-downtime cutover via the sessionfs-infra GitOps pipeline. Residual: lock Cloud Run ingress→INTERNAL_LOAD_BALANCER after this release ships the deploy-api.yml health-check move to api.sessionfs.dev (43b7a0b). Open follow-ups: P5 (self-service seats + full Stripe→entitlement dunning contract); P3 denial-audit durability tk_a0180f0e31794444; the Shield LOW.

**v0.10.32 shipped 2026-06-19 — Stripe `past_due` hotfix (paying customers no longer downgraded on transient declines)**

2279 backend tests (+1) + 388 dashboard UI tests (unchanged). DB migrations: 001–049 (no new migrations). 62 MCP tools (unchanged). Backend-only hotfix. `_handle_subscription_updated` (`src/sessionfs/server/routes/billing.py`) previously treated transient billing-health statuses (`past_due`/`unpaid`/`paused`/`incomplete_expired`) like a cancellation — downgrading to free, zeroing seats/storage, and clearing `stripe_subscription_id` immediately. Since Stripe sets `past_due` on the FIRST failed invoice while smart retries run for days, this cut off paying customers on transient declines AND clearing the subscription pointer orphaned the account from its Stripe customer so the eventual `subscription.updated→active` recovery couldn't re-match. Fix: transient statuses are now non-destructive (keep tier/seats/storage/pointer; log only); ONLY `customer.subscription.deleted` downgrades to free. 3 regression tests (past-due-keeps-access user + org; past_due→active recovery). Found by Ledger during the licensing-redesign review (ticket tk_bd6c9ca29faa4534). Shield-SR CLEAN (0 critical/high). Incidental develop→main catch-up shipped alongside: removal of the unused `dashboard/src/hooks/useAdminLicenses.ts` admin-license hook (no remaining consumers; tsc clean) + chart license-template deltas — pre-existing committed develop state, not new work. NEXT: licensing + org-management redesign (design COMPLETE + fully reviewed — Codex R1→R4 CLEAN + Ledger/Shield/Sentinel APPROVED-WITH-CONDITIONS; P1/P2 cleared to build; see docs/design/licensing-org-redesign*.md + KB #1642/#1656).

**v0.10.31 shipped 2026-06-17 — multi-repo projects (a project can own N git repos) + security dep bumps**

2278 backend tests (+99) + 388 dashboard UI tests (+19). DB migrations: 001–**049** (049 additive: `project_repos` + `merged_into_project_id`/`merged_at`/`repo_reclaimed_at` on projects + `project_merge_audit`). 62 MCP tools (unchanged). Backend + CLI + dashboard. A project now owns N git repos via the `project_repos` join table (each repo in exactly one project, globally-unique normalized remote; existing projects backfilled to a single primary repo so single-repo behavior is unchanged) — personas/KB/tickets/rules are shared across a product's repos instead of duplicated across split projects. **FREE for all tiers** (no tier gate). Shipped on `feat/multi-repo-projects` (P1 migration+models+`services/project_resolver.py` → P2 cutover of 16 resolver/access sites → P3 link/unlink/list-repos API+CLI + verified-ownership → P4 `services/merge.py` merge engine+CLI → P5 dashboard ReposTab/MergeSurface), merged dbe9718. Key mechanics: **F1 verified-ownership** linking (`user_is_project_admin` + a `verification_method` stamp; `github_app` installation proof against the linker's OWN installation; `owner_attested` fallback; `provider_repo_id` server-derived; verified-beats-unverified DISPLACEMENT → emptied holder enters `repo_reclaimed` orphaned state, revived by linking a new repo); **resolver** `resolve_project_by_remote/_by_id` (join-first + legacy fallback + tombstone redirect, hop-cap `ProjectResolutionLoopError`, resolves-but-never-authorizes); **merge** = single atomic txn (SELECT FOR UPDATE on source+target; dry-run default zero-write; per-table matrix: primary-demote → reassign → persona legal-slug collisions → KnowledgeLink key-before-mutate dedup → wiki slug+revisions → ProjectRules promote/archive → final sessions UPDATE → tombstone; durable started→completed/failed audit + denial audit via fresh session surviving rollback). Review: design Codex R5 CLEAN + Sentinel APPROVED + amendment re-review; code Codex R1→R3 CLEAN + Shield-SR APPROVED-WITH-CONDITIONS (conditions implemented+tested). KB #1624. Security: starlette>=1.3.1, python-multipart>=0.0.31, cryptography>=48.0.1 (fresh CVEs) + site vite/astro/js-yaml. Open follow-ups: Forge durable multi-replica edge rate limiting (tk_d2646c1a38174eec); a true 2-connection PG concurrency test for the merge SELECT FOR UPDATE (cloud-deploy verify; SQLite best-effort). Design/security docs internal in docs/design/ + docs/security/.

**v0.10.30 shipped 2026-06-14 — complete dashboard visual redesign (dark-first "mission control") + brand tagline shift**

2179 backend tests (unchanged) + 369 dashboard UI tests (+171 from v0.10.29's 198). DB migrations: 001–048 (no new migrations). 62 MCP tools (unchanged). Frontend + product-site ONLY — no backend, schema, auth, or MCP-tool changes. The dashboard redesign (`feat/dashboard-redesign`, ~60 commits) merged to develop alongside the site tagline branch (`feat/site-tagline`, merged FIRST). Scope: (1) **design-token foundation** — dark-first OKLCH ladder + canonical named type scale (`text-2xs`→`text-lg`) + radius scale in Tailwind v4 `@theme`; ~138 inline styles + ~1455 arbitrary `[var(--…)]` classes systematized onto semantic utilities (KB #1332). (2) **`ui/` primitive library** — Button/Card/Input/Select/Dialog/Tabs/Table/Dropdown/Tooltip/Kbd; the Select/Dropdown is a custom anchored `aria-activedescendant` listbox fixing the detached far-right native popup. (3) **grouped left sidebar** replacing top nav + hero empty states + compact resume bar. (4) **Sessions/Projects list-grid toggles**. (5) **captured-session conversation redesign** — tool-aware ContentBlock (terminal wells, real diffs, chips, sunken result wells w/ error detection + line-clamp + copy) replacing raw `JSON.stringify`; real transcript with role accent rails + chips + themed react-markdown (no `prose-invert`); de-emphasized collapsible thinking. (6) **account/avatar menu** Settings·Help·Logout (primary nav stays sidebar-only). Brand tagline → "Memory Layer For AI Agents" (dashboard + site). Two prod bugs fixed: SessionDetail white-screen crash (useMemo after early-return guards → "Rendered more hooks"; hoisted + added a loading→loaded regression test that catches the class — KB #1336) and the avatar menu missing Settings. Recurring nested-button a11y/hydration class documented (KB #1335). Security: `react-router-dom`/`react-query`/`react-markdown` moved devDeps→deps so `npm audit --omit=dev` covers them (react-router 7.17.0, 2 HIGH cleared); `site/` esbuild override `>=0.28.1` clears 6 HIGH transitive build-time advisories. Consolidated Codex review: prior phases R3-CLEAN at ebc7823; post-CLEAN delta R1 CLEAN + R2 CLEAN (lint typing) + R3 CLEAN (avatar menu). Shield-SR CLEAN — 0 CRITICAL/HIGH.

**v0.10.29 shipped 2026-06-11 — named auth profiles (multi-account on one device) + cloud dashboard 428 CORS fix**

2179 backend tests + 198 dashboard UI tests (+26 from v0.10.28). DB migrations: 001–048 (no new migrations). 62 MCP tools (unchanged). Two workstreams: (1) `tk_457d060822bc48c0` named auth profiles — new `src/sessionfs/profiles.py:resolve_auth()` single resolver unifying the historical `_get_api_config` (env>config) vs `_load_sync_config` (config-only) split, so one identity switch governs coordination + sync + daemon consistently. Precedence SESSIONFS_API_KEY > SESSIONFS_PROFILE > `~/.sessionfs/active_profile` > default config.toml (zero migration). Named profiles at `~/.sessionfs/profiles/<name>.toml` with isolated store_dir + deleted.json; profile-name regex validation (path-traversal defense); CLI `sfs auth login --profile|use|profiles|whoami`; daemon pins startup profile (no mid-run identity flip); capture_guard checks profile-scoped exclusions via new `LocalStore.store_dir` property. Codex R1 (1 HIGH + 2 MED + 1 LOW) → R2 (1 MED) → R3 VERIFIED-CLEAN. (2) `tk_1a73a7bf359f41c3` cloud-only dashboard 428 on Settings → Context/Knowledge Injection — root cause was a CORS expose_headers gap (ETag hidden from cross-origin JS per Fetch spec → dashboard replayed `If-Match: ''` → 428; Helm same-origin masked it). Fix: `expose_headers=["ETag"]` + 2 drift-guard tests. Original empty-rules-row theory disproven by the Helm-works-cloud-fails signal. Shield-SR CLEAN — 0 CRITICAL/HIGH/MEDIUM, 1 LOW follow-up (`sfs security scan` should cover profiles/*.toml perms); Shield hardened raw-key writes to atomic 0600 (`_write_private_file` via os.open) in profiles.py + cmd_cloud.py and bumped react-router 7.13.2→7.17.0. NOTE: the dashboard redesign (feat/dashboard-redesign worktree branch, 18 commits, Codex R2 CLEAN, 270 dashboard tests) is NOT in this release — it merges to develop next and ships as v0.10.30.

**v0.10.28 shipped 2026-06-03 — coordination + recovery surface: ticket mutation verb, org invite resend, admin key recovery, /compile latency cut (4 R2-CLEAN tickets bundled)**

2153 backend tests + 198 dashboard UI tests (+34 from v0.10.27). DB migrations: 001–048 (048 strictly additive: `ticket_edits` audit table for the new update_ticket verb). 62 MCP tools (+1: `update_ticket`). Four Codex-reviewed tickets ship together: (1) `tk_835a876529de4551` update_ticket verb — PUT `/api/v1/projects/{pid}/tickets/{tid}` with creator/project-admin authz, optional lease_epoch fencing (atomic UPDATE WHERE epoch=N with rowcount-1 guard; 409 envelope on stale), per-field `TicketEdit` audit rows (JSON-encoded old/new, edited_by_user_id, edited_by_persona, lease_epoch at edit time), auto-posted `system` diff comment (no-op writes do NOT post — audit-pollution anti-pattern), `depends_on` full-replacement with dedup + `_validate_dependencies_same_project` + `_check_dependency_cycle` BEFORE wipe, lease_epoch-only payload rejected 400. MCP tool excludes `assigned_to`/`related_sessions` (use `assign_persona`). CLI `sfs ticket update` with `--title`/`--description[-file]`/`--priority`/`--acceptance-criteria-file`/`--lease-epoch`. R1 (2 MED + 1 LOW) → R2 CLEAN. (2) `tk_4afbae8ed3a442e9` admin mint-key — `POST /api/v1/admin/users/{user_id}/api-keys` for lost-key recovery (chicken-and-egg unblock: no magic-link/OAuth/reset). Returns raw key once; audit-logged `mint_api_key_on_behalf`; 403 inactive user (no disabled-account backdoor); `key_prefix`/`key_kind`/`scopes`/`created_by_user_id` set explicitly. R1 (1 MED + 1 LOW) → R2 CLEAN → R3 docstring CLEAN. (3) `tk_d88678e6fe384de6` org invite UPSERT — `uq_org_invites_org_email` vs route-predicate mismatch (the production block on richardmcmullen1992 + sammykezz re-invites). UPDATE-in-place over stale rows with `.with_for_update()` row-lock (PG serializes; SQLite single-writer); regenerates `id` to invalidate stale email-acceptance links; preserves `created_at` as audit signal. R1 (1 MED + 1 LOW) → R2 CLEAN. (4) `tk_5c124c496ea14ecc` `/compile` single-pass — dropped the second `auto_generate_concepts()` call (was flagged `(Legacy) ... for compatibility` in its own inline comment); halves LLM latency on real-compile. R1 CLEAN (1 non-blocking test-tightening) → R2 CLEAN. Shield-SR CLEAN — 0 CRITICAL / 0 HIGH / 0 MEDIUM; PyJWT 2.12→2.13 for PYSEC-2026-175/177/178/179. GH #50/#51/#16 closed.

**v0.10.27 shipped 2026-05-30 — daemon liveness hotfix + tier-aware per-file cap discovery (3 R2-CLEAN tickets bundled)**

2119 backend tests + 198 dashboard UI tests (+11 from v0.10.26). DB migrations: 001–047 (no new migrations). 61 MCP tools (unchanged). All additive — no schema changes, no breaking changes, no auth changes. Three Codex-reviewed tickets ship together: (1) `tk_4abfa69b38d54bc0` daemon hotfix — `is_excluded(session_id)` guard inside `_sync_sessions()` short-circuits + discards from `_pending_sessions` before pack/push; closes parent Issue `tk_714456298d424202` (customer C 75 MB excluded-session retry storm, 862+ retries / 4 days, /health timeouts, 13 liveness kills). R1 1 LOW (ruff hygiene) → R2 VERIFIED-CLEAN. (2) `tk_bb56f6a56da34b05` server — `SyncSettingsResponse.max_member_bytes: int` field sourced from `_member_size_limit_for_tier(get_effective_tier(user, db))` (NOT a duplicate constant — tracks SFS_MAX_SYNC_MEMBER_BYTES_{FREE,PAID} operator overrides). R1 first-round CLEAN. (3) `tk_d5945c4bce3245ce` CLI — `_resolve_max_member_size(api_url, api_key)` resolver with precedence env-var > server > 50 MB literal fallback + per-process cache + older-server graceful handling; wired into push / pull / sync_all / handoff / pull_handoff entry points. R1 1 MED (sync_all._push_one skipped preflight) → R2 VERIFIED-CLEAN. Shield-SR pre-release security review CLEAN (0 CRITICAL / 0 HIGH / 0 MEDIUM, 0 npm/pip-audit vulnerabilities). Tag `v0.10.27` on main.

**v0.10.26 shipped 2026-05-30 — typer 0.26 vendored-click compatibility hotfix (unblocks Deploy API for v0.10.24's feature bundle)**

2108 backend tests + 198 dashboard UI tests. DB migrations: 001–047 (047 strictly additive: `kind` enum + `parent_ticket_id` on tickets). 61 MCP tools (+3 from v0.10.23: `rename_session`, `update_persona`, `delete_persona`). v0.10.26 catches both `click.exceptions.ClickException` AND `typer._click.exceptions.ClickException` in `handle_errors` (`src/sessionfs/cli/common.py`); typer 0.26 vendors its own click module so the two class hierarchies are disjoint and the original `except click.exceptions.ClickException` no longer caught typer's `BadParameter`. v0.10.25's `click<8.4` pin was a misdiagnosis — CI installed click 8.3.3 within the pin and tests still failed; the real fix tracks typer's vendored module via best-effort import. All pipelines green: PyPI 0.10.26, api.sessionfs.dev live on 0.10.26, MCP server + Container Images shipped, GitHub Release tag `v0.10.26` on main as `39daa4c`. v0.10.25 PyPI package exists but should NOT be used (handle_errors decorator still broken).

**v0.10.24 (bundled into 0.10.26) — feature bundle: Issue/Task rollup + 2 GH-issue unblocks + dashboard Issue/Task surface**

8 feature areas across 14 commits: (1) **Session rename** (`tk_cf9f1691091d4e8e`) — PATCH endpoint hardening + click-to-edit title/alias dashboard UI side-by-side per CEO + new MCP `rename_session`. (2) **CORS If-Match** (`febd732`) — Dashboard PUT /rules unblocked by adding If-Match + If-None-Match to allow_headers. (3) **Issue/Task rollup** (Atlas `tk_dbccde26ed604b3c` + Prism `tk_5d14f10e489d4361`) — migration 047 adds `kind` enum + `parent_ticket_id`. Forked FSM (Task vs Issue). New `/close` Issue terminator. New `user_is_project_admin` helper drops the user-input-trusting `assigned_to=='compass'` branch (Codex R1 MED #1 fix). MCP + CLI + dashboard parity. (4) **`sfs org create` 500 fix** (`tk_17b39010f9a64cba` / GH #51, `31cec52`) — 1-line `db.flush()` after `db.add(org)` unblocks manual-license enterprise (najitestech). Latent since v0.10.0; masked by Stripe-path autoflush. (5) **Generic IntegrityError envelope** (`tk_e7da4c4508d94bac` / GH #51 ask 2) — PG SQLSTATE + SQLite fallback classification; client envelope strips PII; CLI `format_api_error` helper + dashboard `ApiError` envelope parsing. (6) **Rules max-tokens UX** (`tk_d4a13a68b6724ba6`) — dashboard input max=20000 + structured envelope `max_tokens_exceeded`. (7) **MCP persona update/delete** (`tk_32abb6d0d4744c5d` / GH #50) — strict force parsing (no `bool("false") == True` footgun); reactivation via PUT with is_active=true. (8) **Dashboard Issue/Task surface** (Prism) — single-surface design (no separate IssuesTab); kind filter, Children rollup, Close button, parent breadcrumb. Codex review cycles all VERIFIED-CLEAN. Shield-SR + Scribe-Site CLEAN.

**v0.10.15 → v0.10.23 (bundled history)** — Scout v5 multi-source signal-shape contract (`docs/integrations/scout-signal-shape.md` + 4 n8n normalizer templates), Scout state-cache upsert=true opt-in, ChatGPT MCP `_ConsumedResponse` hotfix, persona name case-insensitive lookup, release-skill Scribe-Site mandatory gate, AgentPersona audit triple migration 044, KnowledgeEntry persona_name + author_class migration 045 with anti-spoof, AgentRunResponse audit-triple exposure, org invite email + dashboard /invites with atomic accept/decline FSM (migration 046), `auth.project_access` user-key 3-branch predicate (owner OR org-member OR captured-session — closes Phase 5 design intent), starlette>=1.0.1 pin (GHSA-86qp-5c8j-p5mr), `_safe_entry_link_ids` defensive helper, `_auto_supersede` pair-dedup, `auto_generate_concepts` flush-before-add, `knowledge_health` 3-bucket counts. v0.10.14 was the `_prune_dead_concept_pages` int(source_id) defensive guard. See CHANGELOG.md for per-release detail.

**v0.10.13 complete — fail-closed /rebuild + /compile + R5 admin repair endpoint (incident response)**

**v0.10.13 complete — fail-closed /rebuild + /compile + R5 admin repair endpoint (incident response)**

1925 backend tests + 186 dashboard UI tests. DB migrations: 001–042 (no new migrations). 58 MCP tools (unchanged). **v0.10.13** is the safety release forced by the 2026-05-20 incident on the SessionFS dev project (proj_c0242b0fccbd48b4) where `/rebuild` wiped `context_document` and every active claim's `compiled_at` when the recompile crashed mid-flight. Two fixes ship together. (1) `tk_bc3c02a63e994717` (CRITICAL) — `compile_project_context` previously committed twice inside the function (lines 419 + 522), and the `/rebuild` route added two more commits BEFORE calling it for the destructive resets. Four transaction boundaries through one logical compile, with destructive resets in the first two — any crash after them was data-destructive. Refactored to single-atomic-commit shadow-write: Phase 1 reads only → Phase 2 compute in memory (LLM call here, no DB lock contention beyond the project row lock already held since Phase 1) → Phase 3 single atomic transaction with all writes (freshness/decay/retention/auto-promote UPDATEs, force_rebuild's NULL compiled_at + clear context_document if applicable, projection writes, ContextCompilation INSERT, section page upserts) committed once. The pre-flush at the projection step only populates compilation.id WITHOUT making writes durable. /rebuild is now a thin wrapper calling compile_project_context(force_rebuild=True). Codex R1 caught a related correctness bug: force_rebuild=True was nulling compiled_at but the compile still merged new claims INTO project.context_document (stale text), so dismissed/superseded entries' content survived rebuilds forever. Fix: split previous_context (kept for audit on ContextCompilation.context_before) vs compile_base_context (empty when force_rebuild=True so _simple_compile / LLM prompt genuinely starts from scratch). 3 new rollback regression tests in test_knowledge.py including the headline test_rebuild_rollback_on_compile_crash_preserves_prior_state — the test that would have caught the 2026-05-20 incident. (2) `tk_dd3ba7082ef0432e` (HIGH) — new admin repair endpoint POST /api/v1/admin/projects/{project_id}/restore-from-compilation with body {compilation_id: int, dry_run: bool = true}. Reads a ContextCompilation row → parses source_manifest JSON → restores project.context_document from context_after + restores compiled_at on participating entries to compilation.compiled_at. Single atomic transaction. Admin-gated via require_admin; audit-logged via AdminAction. Codex R1 MEDIUM fix: Python's bool-is-int trap on compilation_id (a body {"compilation_id": true} would coerce to compilation_id=1) — fixed via explicit isinstance(x, bool) rejection on both compilation_id and dry_run. 9 integration tests covering dry-run/apply round trip, 404/422 paths, cross-project denial, non-admin rejection, strict bool/non-bool rejection. Codex single review thread tk_879dbd5a5a034d0e: R1 1 HIGH + 1 MEDIUM → R2 VERIFIED-CLEAN. Shield-SR independent pre-release security review CLEAN — 0 CRITICAL / 0 HIGH / 0 MEDIUM new findings. Non-blocking follow-ups deferred per Codex: duplicate freshness pass in /rebuild (cosmetic, return count from compile_project_context later) and pre-existing duplicate concept generation in /compile route (separate ticket).

**v0.10.12 complete — bulk-promote eligible KB notes + review-state row cap + release-process hardening**

1912 backend tests + 186 dashboard UI tests. DB migrations: 001–042 (no new migrations). 58 MCP tools (+1 `promote_eligible_entries`). **v0.10.12 (Codex R1 1 MED + 1 LOW → R2 VERIFIED-CLEAN on bulk-promote review tk_03263e280f4b4732; v0.10.12 follow-ups admin-closed per CEO since Codex pre-flagged them non-blocking).** Headline: bulk-promote service (`src/sessionfs/server/services/bulk_promote.py`) + endpoint (`POST /api/v1/projects/{pid}/entries/bulk-promote`) + MCP tool (`promote_eligible_entries`) + CLI (`sfs project promote-eligible`) — closes tk_c64915570f4d4042, the v0.10.10 confidence-bug cleanup wedge. Dry-run default; --confirm to mutate. Eligibility: class=note + not dismissed + not superseded + optional entry_type + min_length + min_confidence (or caller's set_confidence override) + no near-duplicate against active claims. In-run promotions join the compare set so two near-dups can't both promote in one pass. Pre-fetches 50 active claims once for the dup check; trivial CPU work for 270+ candidates. **review-state row cap** (tk_33a25a12a5cf4dc3, Shield-SR LOW): `GET /tickets/{tid}/review-state` LIMIT 500 TicketComment rows ORDER BY (created_at, id) ASC so earliest review rounds always survive. **Release-process hardening** (tk_cd196cf0421b4a6e + tk_b9b6eb47685e4916 + tk_4698db39fb0248b3): pinned mypy>=1.20,<2.0 in pyproject [project.optional-dependencies] dev (matches CI; v0.10.11's union-attr lint surprise can't recur); new `.release/sanitize_main.py` deterministic Python helper for develop→main sanitization with --dry-run default + --apply + 8 unit tests; new release-skill step 12b post-PyPI smoke (fresh-venv `pip install sessionfs==X.Y.Z` + `sfs --help` shape + new-command help check with 6×30s retry for PyPI index lag). Shield-SR independent pre-release security review CLEAN — 0 CRITICAL / 0 HIGH / 0 MEDIUM new findings + 9 dependency CVEs auto-upgraded (cryptography 46.0.5→48.0.0 with `>=46.0.7` pin widening, idna 3.11→3.15, mako 1.3.10→1.3.12, pygments 2.19.2→2.20.0, urllib3 2.6.3→2.7.0, pip 26.0→26.1.1).

**v0.10.11 complete — service-keys CLI + docs/api-keys.md + review-state derivation + DNS error fix**

1871 backend tests + 186 dashboard UI tests. DB migrations: 001–042 (no new migrations). 57 MCP tools (+3 from v0.10.10: `update_entry_confidence`, `promote_entry`, `get_ticket_review_state`). **v0.10.11 (full receipts across 5 separate Codex review threads, all VERIFIED-CLEAN):** Phase 1 service-keys CLI (parent tk_e0d7db15ff814c0a, review tk_53e042ecee7e43ff R1 1-MED + 1-LOW → R2 CLEAN) — `sfs admin service-keys list|create|revoke|rotate|scopes` + `sfs auth keys list|create|revoke` wraps the v0.10.10 backend; raw key returned once on create/rotate with `--output-key` flag for CI capture; local scope validation rejects `*` wildcard + unknown scopes pre-network; structured error parsing covers all 4 FastAPI envelope shapes. Phase 2 `docs/api-keys.md` (parent tk_522991717c6446c9, review tk_ee765a03b69045eb R1 1-MED + 3-LOW → R2 CLEAN) — public reference for the 14-scope vocabulary with current opt-in status (only handoffs:write and agent_runs:write are live; 12 remaining scopes reserved for Phase 3 route opt-in); CLI reference gets full option tables for both new commands. Phase 3 DNS-error catch (parent tk_aeb8580706d84e2e, review tk_d611718c24944110 R1 CLEAN first round) — `_api_request` catches httpx.RequestError (DNS / timeout / network / SSL but NOT HTTPStatusError) and prints `Configure cloud auth first: sfs auth login` instead of raw Python traceback; benefits every CLI using the shared helper. Phase 4 review-state derivation (parent tk_e025375272b84a95, review tk_d7354a8032e8443b R1 CLEAN first round) — `GET /api/v1/projects/{pid}/tickets/{tid}/review-state` derives open/closed findings + last verdict + severity counts from comment thread without LLM; new `get_ticket_review_state` MCP tool (56 → 57) + `sfs ticket review-state <id>` CLI; closure rule: findings raised in round N close when any subsequent round has VERIFIED-CLEAN verdict. Bonus fixes: shared `_api_request` now forwards DELETE bodies via `client.request("DELETE", ..., json=...)` and handles 204 No Content cleanly; dashboard footer now reads from `dashboard/VERSION` checked-in file (was hardcoded v0.9.9); manifest JSON schema accepts v0.10.4 `retrieval_audit_id` field. Shield-SR independent pre-release security review CLEAN — 0 CRITICAL / 0 HIGH / 0 MEDIUM new findings. One LOW noted as Atlas follow-up tk_33a25a12a5cf4dc3 (cap row count on review-state endpoint).

**v0.10.10 complete — scoped service API keys for cloud agents + KB confidence fix**

1819 backend tests + 186 dashboard UI tests. DB migrations: 001–042. 54 MCP tools (unchanged from v0.10.9). **v0.10.10 (full receipts across 4 separate Codex review threads, all VERIFIED-CLEAN):** scoped service API keys parent ticket `tk_2e030a85253143df` shipped across 2 phases — Phase 1 foundation review (tk_9fefcc3832ac49da, R1 scope + R2 schema + R3 implementation + R4 re-review CLEAN) and Phase 2 route opt-in (tk_f664b480140f40c4, R5 + R6 + R7 CLEAN). Plus tk_483cede83deb443b KB confidence fix (Codex tk_328006e4c6024dd8 R1-R3 CLEAN) and tk_32f3dacf1c9749bc list_ticket_comments MCP tool (R2 CLEAN, bundled from v0.10.9 cycle). Migration 042 extends ApiKey with key_kind ('user'|'service'), org_id (FK organizations CASCADE, required for service keys), scopes (JSON list, '*' wildcard reserved for legacy user keys), expires_at, revoked_at + revoke_reason, created_by_user_id (FK users SET NULL), last_used_ip (45 chars IPv6-safe), service_key_name, project_ids (optional per-key allowlist), key_prefix (real raw-key prefix captured at create/rotate). 5 audit-row tables (TicketComment, KnowledgeEntry, AgentRun, RetrievalAuditEvent, HandoffEvent) gain actor_type + service_key_id + service_key_name so service keys never silently impersonate humans. New auth dependencies: AuthContext dataclass + require_scope(*scopes) factory + assert_service_key_can_access_project helper + assert_service_key_handoff_boundary helper. get_current_user REJECTS service keys with 403 service_key_not_allowed (deny-by-default, pre-route enforcement — Codex Phase 2 R2 HIGH fix). require_scope is the ONLY dependency that admits service keys. tier_gate.get_user_context refactored to source from request.state.auth_context (avoid double-rejecting service keys on routes that opt in). 5 handoff write routes (create/claim/revoke/decline/comments POST) + agent_runs (create/complete) converted to require_scope. Cross-org boundary resolved via session.project_id (authoritative; Codex Phase 2 R6 HIGH fix — earlier git_remote_normalized 'prefer org_id match' was a bypass vector). Orphan-handoff mutation by service keys explicitly denied. 14 scope catalog: sessions:read, handoffs:read/write, tickets:read/write, personas:read/write, knowledge:read/write, rules:read/write, agent_runs:read/write, retrieval_audit:read, admin:*. Structured error codes: api_key_revoked, api_key_expired, service_key_not_allowed, insufficient_scope (with required+current arrays), cross_org_denied, service_key_project_required, service_key_project_not_registered, service_key_project_ambiguous. Org-scoped admin surface: POST/GET /api/v1/orgs/{org_id}/service-keys + DELETE + rotate (org admin role + Team+ tier). Personal user-key surface at /api/v1/auth/me/api-keys. Raw key returned exactly ONCE on create + rotate, persisted as key_prefix for list/detail. KB fix (tk_483cede83deb443b + Codex R3 CLEAN): root cause was confidence = min(confidence, 0.7) silent clamp in /entries/add for cli-ask/manual sources (combined with MCP's default session_id='manual' this silently lowered every caller-supplied confidence). Fix: AddEntryRequest.confidence is Optional[float] with Field(None, ge=0.0, le=1.0); server honors explicit values, only applies 0.7-for-manual default when caller omits. MCP _handle_add_knowledge only forwards confidence when caller passed it. NEW PUT /api/v1/projects/{pid}/entries/{id}/confidence as repair path for legacy entries already stuck at 0.7. /compile no-op response now derives context_words_before/after from project.context_document (matches health) and includes noop_reason explaining why nothing was compiled. New _entry_to_response shared serializer. Shield-SR independent pre-release security review CLEAN — 0 CRITICAL / 0 HIGH / 0 MEDIUM. Release approved. **v0.10.9 (full receipts on parent `tk_89e90060e6314311` across 5 rounds Codex review R1-R4 + R3 close re-verification + tk_731053e14fd24031 review thread + tk_32f3dacf1c9749bc tickets-MCP follow-up):** comprehensive handoff redesign — migration 041 (12 new Handoff columns: recipient_user_id, recipient_team_id, ticket_id, persona_name, revoked_at, revoked_by_user_id, revoke_reason, handoff_kind, snapshot_persona_name, snapshot_ticket_title, sender_tier_snapshot, viewed_at; 5 new tables: teams, team_members, handoff_attachments with project_id for unambiguous wiki slug lookup, handoff_comments, handoff_events). Exactly-one-recipient invariant (email XOR user_id XOR team_id) enforced via Pydantic @model_validator + strip_blank field_validators (R2 LOW #4). New routes: POST/{id}/revoke (sender-only, atomic, required reason), POST/{id}/decline (recipient-only, atomic), POST/GET/{id}/comments (sender + valid recipients), GET/{id}/events (audit log). Extended POST / (new fields + provenance + attachments), GET /{id} (404-not-403, lazy expiry, viewed_at on first recipient GET), POST/{id}/claim (atomic UPDATE WHERE status='pending' rowcount-1 race guard, blob copy AFTER race won so losers never write orphans, active_ticket_payload with project_id derived from ticket OR from source session for persona-only handoffs, dropped_attachments via validate_attachments_for_recipient using stored project_id, R3 HIGH 1 — eligibility before lazy_expire+status). New routes/teams.py (POST/GET /teams, GET/DELETE /teams/{id}, POST/GET/DELETE /teams/{id}/members/...) with check_role(admin) + team_management (Team+) + cross-org 404 isolation + target-user-must-be-org-member. tier_gate: team_handoff added to TEAM + ENTERPRISE. 4 lifecycle email templates (handoff_claimed/revoked/declined/comment) using html.escape on user input, best-effort try/except. 8 new MCP handoff tools (53 total) using shared _handoff_api_config helper. CLI: sfs handoff extended with --to-user-id/--to-team-id/--ticket/--persona/--expires-hours/--attach kind:ref_id; 6 new subcommands (sfs handoffs get/revoke/decline/comment/comments/events). pull_handoff persists active_ticket_payload to ~/.sessionfs/active_ticket.json (R3 HIGH 2 fix — the core v0.10.9 promise). HandoffAttachmentResponse includes project_id (R3 MEDIUM 4 — multi-project recipient disambiguation). /summary rewritten to claim_inbox_match + 404 (R3 MEDIUM 3). Deferred to v0.10.10+ per Codex: broadcast claim_policy, parent_handoff_id relay chain, blob_refcount, background sweeper, team email fan-out for revoke/comment. **Follow-up tk_32f3dacf1c9749bc (bundled here as same-cycle work):** new list_ticket_comments MCP tool wrapping GET /tickets/{id}/comments. REST endpoint extended with `since` (ISO timestamp) + `since_id` (cursor tiebreaker — Codex caught that pure since-only filter skipped same-millisecond siblings, fixed with `(created_at, id) > (since, since_id)` tuple comparison + ORDER BY (created_at, id)) + `limit` (1-500, default 200). MCP handler validates args BEFORE _resolve_project_id so bad input surfaces locally, not as DNS error. Shield-SR independent pre-release review CLEAN — 0 CRITICAL / 0 HIGH / 0 MEDIUM. **v0.10.8:** test-only fix — `test_returns_kb_and_session_sources` failed on the public CI runner because `_fetch_kb_entries_raw` re-imports `load_config` from `sessionfs.daemon.config` inside the function body, bypassing the monkeypatch on `mcp_server.load_config`. Locally the test passed because the dev's real `~/.sessionfs/config.toml` had a non-empty api_key. CI without that file → empty api_key → early return → empty `sources_cited` → assertion fail → Deploy API + Deploy MCP Server failed → Cloud Run stayed at v0.10.6. Fix: also patch `sessionfs.daemon.config.load_config` directly. No product code changes from v0.10.7. Version 0.10.7.1 was attempted but Helm rejects 4-segment versions (strict SemVer); jumped to v0.10.8 instead. **v0.10.7 (full receipts on parent `tk_a1144426a013413c` across 8 rounds Codex review R1-R8 + migration sub-ticket `tk_bb30dd4ed9794d68` + watch sub-ticket `tk_c72ad4f99dae404b`):** typed `sources_cited: list[{type: "kb"|"session", id}]` on ask_project (structured re-fetch via `_fetch_kb_entries_raw`, no regex from prose). Migration 040 — `wiki_page_revisions` table (UniqueConstraint inline at `op.create_table` for SQLite compat after R7 pivot from a follow-up-migration approach Codex correctly identified as unreachable on a halted SQLite chain) + `session_summaries.personas_active` column. New `GET /api/v1/projects/{id}/pages/{slug}/history` endpoint with `next_cursor` envelope + `get_wiki_page_history` MCP tool (45 → 46 tools). Wiki PUT `PageWriteRequest.persona_name`/`ticket_id` plumbing; `_validate_revision_provenance` rejects forged provenance (project-scoped persona check + ticket-ownership via creator OR resolver OR active-executor with `RetrievalAuditContext.lease_epoch == ticket.lease_epoch AND ticket.status == 'in_progress'` — closed_at gate dropped because nothing in the codebase ever sets it). MCP `update_wiki_page` auto-threads persona/ticket from active-ticket bundle. `personas_active` on `SummaryResponse` refreshed on both deterministic + narrative regen paths. Org setting `require_lease_epoch_on_ticket_writes` enforces 422 on omitted lease at complete/comment/accept; personal projects bypass. New CLI `sfs ticket watch <id>` (poll comments endpoint, `--interval` clamped [5, 300], `--from-author`, `--exit-on-new`, `--notify` macOS terminal-notifier; pairs with v0.10.5 `sfs ticket comments`). Test-isolation flake fix (alembic env.py's `fileConfig` was killing `sfs.writeback` logger propagation under full-suite runs — fixture now skips fileConfig). Shield-SR independent pre-release review CLEAN — 0 CRITICAL / 0 HIGH / 0 MEDIUM. Two follow-up tickets filed during this work: `tk_7dc9e8764a5a4297` (broader SQLite migration chain — 003+ uses PG-only `USING GIN`/JSONB/etc; smoke test xfail-strict until landed). **v0.10.6:** added the Kilo Code watcher as the 9th supported tool. `KiloCodeWatcher` is a thin subclass of `ClineWatcher` (~35 lines) — Kilo Code is a fork of Roo Code which is a fork of Cline, and the per-task UUID storage layout is identical, so all parsing logic delegates to the already-reviewed Cline path. Wired into `daemon/main.py`, `sfs init` auto-detect (storage at `globalStorage/kilocode.kilo-code/`), `sfs watcher enable kilo-code`, `sfs mcp install --for kilo-code` (extension ID `kilocode.kilo-code` — marketplace `kilocode.Kilo-Code` is case-folded by VS Code), `sfs recapture`, and the `sfs resume --in kilo-code` capture-only rejection path. 7 new regression tests in `tests/unit/test_kilocode.py`. Shield-SR independent pre-release review CLEAN — 0 CRITICAL / 0 HIGH / 0 MEDIUM. **v0.10.5 (full receipts on ticket `tk_12e6d8775eb045a2` across R1 + R2 of Codex review + KB 396 for the archive change):** extended `get_context_section.source_entries` with `created_by_persona` (from the source Session's persona_name, resolved via batched SELECT scoped to `project_id` AND `is_deleted == False`) and `compile_id` (denormalized from parent `ContextCompilation.id` at read time with `ORDER BY (compiled_at DESC, id DESC)` tiebreak). Tier-aware `sync/archive.py` (`validate_tar_archive` + `unpack_session` accept `member_limit_bytes`, default 100 MB safety ceiling matching server `_validate_tar_gz`, CLI threads `MAX_MEMBER_SIZE`/`SFS_MAX_SYNC_MEMBER_BYTES_PAID` through pull/sync/handoff). New CLI `sfs ticket comments <id>` (read-only client of existing GET endpoint, renders Markdown body in titled Panels). Site `devalue` 5.6.4 → 5.8.1 (GHSA-77vg-94rm-hx3p CWE-770 DoS HIGH). Shield-SR independent pre-release review CLEAN — 0 CRITICAL / 0 HIGH / 0 MEDIUM. **v0.10.4 enterprise hardening (full receipts on tickets `tk_13124010a49745d9` + `tk_b3ee62c732c44594`):** migration 039 (tickets.lease_epoch + context_compilations.source_manifest + sessions.retrieval_audit_id + retrieval_audit_contexts table + retrieval_audit_events table). Ticket lease fencing (atomic UPDATE lease_epoch+=1 on start; complete/comment/accept enforce supplied lease_epoch via WHERE-clause predicate; 409 on mismatch; opt-in semantics documented on MCP tool descriptions). Compile source manifest (snapshots active KB claims feeding each section before LLM compile; get_context_section returns source_entries for SoD trace). Retrieval audit (server-primary at /api/v1/{projects/{id}/retrieval-audit-contexts, projects/{id}/retrieval-audit-events, retrieval-audit-contexts/{ctx}/events, sessions/{id}/retrieval-log}; local JSONL fallback for offline MCP clients; SAFE_AUDIT_ID_RE path-traversal defense; sanitize_arguments strips api_key/token/secret/password/auth/credential fragments; collect_returned_refs depth-limited at 50, no regex-extraction from prose; cross-project validation on every linkage; session upload validates retrieval_audit_id against project + owner; context create validates ticket_id + persona_name; payload caps at 16KB with _truncated marker; local JSONL caps at 10MB). New MCP tool `get_session_retrieval_log` (44 tools total, was 43). New CLI command `sfs persona pull [<name>|--all]` syncs server personas to .agents/<name>-<role-fragment>.md preserving the kebab convention, HTML-comment preamble only (no double H1). Site messaging rewrite (memory/identity/coordination/governance pillars + "Your team today. AI agents tomorrow." enterprise section). Shield-SR independent pre-release review CLEAN — zero CRITICAL/HIGH findings; one LOW UX inconsistency noted as Atlas follow-up. **v0.10.3 Dashboard UI (full receipts on ticket `tk_530dfba7f14446dd` across 2 rounds of Codex review):** three new ProjectDetail tabs surfacing the v0.10.1 personas + tickets and v0.10.2 AgentRun backends — PersonasTab (list / create-edit-delete modals with name-immutable ASCII regex + `--force` non-terminal-ticket guard toggle), TicketsTab (list filtered by 7-state FSM + All, expand-in-place detail with acceptance criteria ☐ boxes + dependencies + completion notes + comments, approve/dismiss buttons gated by current status, inline comment composer, new-ticket modal), AgentRunsTab (audit-trail read-only view with status/persona/trigger filters, 30s refetchInterval, expand to view tool/ticket/trigger ref/fail-on/duration + clickable CI URL through `safeHttpUrl(raw)` http/https allowlist + structured findings JSON via `<pre>` text node). API client adds 14 new methods; 3 new react-query hooks (`usePersonas`, `useTickets`, `useAgentRuns`); ProjectDetail tab union extended with `personas | tickets | agent-runs`. Lifecycle FSM transitions (start/complete/resolve/escalate/block) intentionally stay CLI/MCP-only because they need the local active-ticket bundle. Also fixed: CLI `sfs agent complete --findings-file` rejects non-object list elements locally with element-index message; `handle_errors` decorator preserves `typer.Exit(N)` exit codes (was downgrading all to exit 1 with "Unexpected error: N" because `typer.Exit` is `RuntimeError`, not `SystemExit`); `sfs agent run` polling-loop mypy union-attr error via `isinstance(r3, dict)` narrowing + pre-init; dashboard `addToast({kind, message})` → `addToast(type, message)` across 10 sites (was failing TS2554 in `tsc -b`); `ci_run_url` scheme guard via `safeHttpUrl()` allowlist with `rel="noopener noreferrer"`; DeleteConfirmModal Esc-close handler. Shield-SR independent pre-release review CLEAN. **v0.10.2 AgentRun + CI Integration (full receipts on ticket `tk_f5381e113f144be5` across 10 rounds of Codex review):** AgentRun layer on top of v0.10.1 personas + tickets — migration 038 (`agent_runs` table, project-scoped FK, plain-string `persona_name`/`ticket_id`/`session_id` for audit-row survival, JSON-as-text findings with `NOT NULL DEFAULT '[]'`, five indexes), REST API (`POST/GET/start/complete/cancel` at `/api/v1/projects/{id}/agent-runs` with atomic `UPDATE ... WHERE status IN (...)` rowcount-1 guards, same-project ticket validation, errored/failed final statuses force `exit_code=1`, **Tier: Team+ (`agent_runs`)**), CLI (`sfs agent run/complete/status/list` with machine-safe `--output-id` and `--enforce` defense-in-depth on failed/errored), MCP tools (7 new: `create_agent_run`, `complete_agent_run`, `list_agent_runs`, `approve_ticket`, `checkpoint_session`, `list_checkpoints`, `fork_session`; total 36 → 43), `sfs ticket create --output-id` companion flag, env-var auth (`_get_api_config` honors `SESSIONFS_API_KEY` / `SESSIONFS_API_URL` so CI runners authenticate without prior `sfs auth login`), manifest schema additions (`persona_name`/`ticket_id`/`instruction_provenance`/`_resume_parent_id`), tool-aware token budgets adds `bedrock` (16k) + `vertex` (8k). CI examples (`docs/integrations/github-actions-agent-run.yml` + `docs/integrations/gitlab-agent-run.yml`) carry hardening developed through 10 rounds of Codex review: per-step API-key scoping (off the PR-controlled review step), `persist-credentials: false` on `actions/checkout`, `permissions: contents: read` only, PR title/body via `env:` (never `${{ }}` template substitution), `jq -e 'type == "array" and all(.[]; type == "object")'` findings-shape guard routing crash/missing/malformed/non-object-element to errored complete so runs never stick in `running`, workspace-relative `.sessionfs/` paths so `hashFiles()` works AND artifacts upload, separate `comment-on-pr` job pattern (GitHub Actions only supports workflow-/job-level `permissions:`, not step-level). Cloud Agent Control Plane (Bedrock action-group dispatcher with closed `OPERATIONS` table + `parse.quote(safe="")` URL escaping, Vertex AI function-calling schema; public docs at `site/src/content/docs/integrations/cloud-agents.mdx`). Shield-SR independent pre-release review CLEAN — zero CRITICAL/HIGH findings, zero pip-audit/npm audit vulnerabilities. **v0.10.0 Org Admin Console (full receipts under KB entity_ref `org-admin-console-v0.10.0-phase-{1..7}`):** full org administration surface across 7 phases — members management (`MembersTab` with CEO data-stays modal copy + last-admin guard + SELECT FOR UPDATE concurrency safety), project transfers between scopes (`POST /api/v1/projects/{id}/transfer` + `POST /api/v1/transfers/{id}/{accept,reject,cancel}` + `GET /api/v1/transfers` + auto-accept on initiator==target + atomic pending→accepted UPDATE with rowcount check + partial-unique index backstop for concurrent initiate races + standing-revalidation on accept), session→project routing (`sessions.project_id` FK from migration 036 + `_resolve_project_id_for_session` shared helper with SELECT FOR UPDATE on Project + OrgMember rows running inside Phase 3 write transaction + re-evaluation on every sync covers pre-Phase-5 sessions retroactively linking + stale-access clearing), multi-org default (`User.default_org_id` + `/api/v1/auth/me` includes it + `PUT /api/v1/auth/me/default-org` with membership validation + `sfs config default-org` CLI + creation-default inheritance for new org-scoped projects copies kb_retention_days/kb_max_context_words/kb_section_page_limit from `Organization.settings.general`), org settings (`GET/PUT /api/v1/orgs/{org_id}/settings` with admin-only PUT + range validation + structural-merge preservation of DLP block + OrgSettingsTab form on dashboard). Dashboard surfaces: `MembersTab` (org members + CEO data-stays modal), `TransferInbox` (`/transfers` page incoming+outgoing with Accept/Reject/Cancel), `TransferPanel` (per-project Transfer tab on Project page with destination dropdown + pending-cancel branch), `OrgSettingsTab` (KB creation defaults form on Organization page), nav `/transfers` link with pending-incoming badge. CLI: `sfs project init --org|--personal` + `sfs project transfer --to|--accept|--reject|--cancel <id>` + `sfs project transfers --direction --state` + `sfs config default-org [<id>|--clear]`. **Phase 6 (v0.9.x) features carried over:** First-run onboarding (auto-login, getting-started page, state-based redirect gate, scoped dismissal). Unified 410 delete propagation (SyncDeletedError, shared cleanup across daemon + CLI + sync, tracked_sessions cleanup). Sort direction toggle (asc/desc). Tool filter alias normalization (gemini/gemini-cli, copilot/copilot-cli). Session Delete Lifecycle (three-scope delete cloud/local/everywhere, sync-aware exclusion list with fcntl locking, `sfs delete/trash/restore` CLI, dashboard DeleteScopeDialog + TrashView, admin purge endpoint, restore scope-aware guidance, migration 030). Rules Portability (canonical project_rules + rules_versions tables with ETag/FOR UPDATE optimistic concurrency, 5 deterministic tool compilers — CLAUDE.md / codex.md / .cursorrules / .github/copilot-instructions.md / GEMINI.md — with managed markers and token-budgeted knowledge/context injection, `sfs rules init/edit/show/compile/push/pull` CLI with managed-file safety and shared-vs-local-only modes, session instruction provenance on manifests with `SFS_CAPTURE_GLOBAL_RULES` toggle, MCP `get_rules`/`get_compiled_rules`, dashboard RulesTab, path-traversal defense in write paths). Resume-Time Rules Sync (`sfs resume` preflights the target tool's rules file from current canonical rules before launch, `--no-rules-sync`/`--force-rules` flags, one-time-permission ownership transition, partial compiles never version canonical history, malformed-payload hardened, non-fatal semantics). Knowledge Base v2 (claim model with evidence/claim/note layers, per-type freshness decay, auto-promotion at compile, supersession with reason + entity_ref, writeback gates with specificity + semantic dedup + rate limit, rebuild endpoint + `sfs project rebuild`, refresh endpoint replacing "Still valid", section pages as true projections of active-claim state, concept page prune fix, used_in_answer tracking). Eight-tool capture + four-tool resume (auto-launch, cross-tool transcript via --append-system-prompt-file, 50-message trim). Session summarization (deterministic + narrative LLM summaries). Autosync (off/all/selective modes, debounce, server-side watchlist sync, conflict re-dirtying). Local storage management (pruning, retention, disk warnings). Multi-provider email (Resend, SMTP, none). Team handoff with email + session copy on claim + status stepper + session context card + recipient session ID + metadata snapshots. LLM Judge with confidence scores (0-100), CWE mapping, evidence linking, dismiss/confirm findings. Shared project context (CLI + API + MCP tool + dashboard page with markdown editor). Living Project Context (auto-summarize on sync, knowledge entries with 6 types, content-level dedup, structured compilation with verified/unverified promotion, section pages, concept auto-generation, regenerate, wiki pages with backlinks, auto-narrative toggle, backfill on project creation). MCP server (local + remote, 21 tools with workspace roots detection + explicit git_remote parameter on all 6 project-scoped tools — Tier A read surface added in v0.9.9.6: get_knowledge_entry, list_knowledge_entries, get_wiki_page, get_knowledge_health, get_context_section, get_session_provenance, compile_knowledge_base). MCP install for all 8 tools (stale registration repair, malformed config handling). Full-text search. GitHub PR App (signature enforcement) + GitLab MR integration (per-user webhook secrets, comment dedup, settings CRUD). Admin dashboard (all 6 tiers, self-demotion guard, org cleanup on delete). Helm chart (EKS validated, single-ingress via nginx, license validation, seed job, hardened security: non-root UID 10001, read-only rootfs, RuntimeDefault seccomp). Self-hosted license lifecycle (migration 017, grace periods, admin CLI + API, dashboard licenses tab). DLP / Secret Scrubbing (14 PHI patterns + 22 secret patterns, BLOCK/REDACT/WARN modes, server-side scan of all archive files, redact_and_repack, org policy via settings JSON, CLI sfs dlp scan/policy, dashboard settings tab + session findings, migration 024). Cursor tool call extraction from agentKv layer. Tier-based sync limits (50MB free, 300MB paid). FSL licensing (ee/ directory). Server-side tier gating (6 tiers, 30+ features). RBAC (admin/member roles, seat enforcement on invite accept). Stripe billing (org-isolated checkout with separate Stripe customer, org-first webhook handling, personal sub detection, portal scope). Organization management (email normalization, seat re-check on accept). Telemetry endpoint. Client version tracking. Dashboard (code-split 262KB main + 12 lazy chunks, command palette search with Cmd+K, mobile nav drawer, focus trapping, ARIA live regions, Zod form validation, project tab transitions, product identity). `sfs init` wizard (auto-detects 8 tools, optional sync). `sfs security scan/fix` (config permissions, API key exposure, dependency audit). `sfs dlp scan/policy`. Security pipeline (GitHub Action: pip-audit, Trivy, Bandit + Dependabot + SECURITY.md). Skill/slash command detection across all converters. Tool call capture for Gemini CLI (toolCalls array) and Amp (tool_use/tool_result blocks). Multi-select bulk delete + Find Duplicates in dashboard. Session deduplication (Codex watcher skips sessionfs_import). Search tier check uses effective org tier. Self-healing SQLite index (auto-rebuild from .sfs files, 0-byte detection). `sfs doctor` (8 health checks with auto-repair). `handle_errors` decorator on all CLI commands. Message pagination (newest-first, order toggle, sidechain/empty filtering). Share links (PBKDF2 passwords via POST body, configurable API URL, alias-aware revoke).


<!-- sessionfs-knowledge-start -->
## SessionFS Knowledge Base — MANDATORY

**IMPORTANT: You MUST call `add_knowledge()` via MCP when you discover something significant about this codebase.** This is not optional — it is a core requirement of working in this repo, just like following commit conventions.

Every discovery you make helps the next AI agent that works here. If you learn it, log it.

### You MUST contribute when you:
- Make or discover an architecture/design decision → `add_knowledge("description", "decision")`
- Find a code pattern or convention → `add_knowledge("description", "pattern")`
- Discover a bug or workaround → `add_knowledge("description", "bug")`
- Learn about an external dependency → `add_knowledge("description", "dependency")`
- Establish a coding convention → `add_knowledge("description", "convention")`
- Learn anything useful for future sessions → `add_knowledge("description", "discovery")`

### How to contribute
- Quick facts: `add_knowledge("what you learned", "type")` — call this multiple times per session
- Full articles: `update_wiki_page("slug", "markdown content")` — for substantial findings
- Search first: `search_project_knowledge("topic")` — avoid duplicates
- Browse: `list_wiki_pages()` — see what exists

### Examples of when to call add_knowledge:
- "The auth middleware resolves effective tier from the org, not the user record" → decision
- "All converters follow the pattern: parse native format → canonical .sfs → write" → pattern
- "SQLite WAL mode can corrupt on power loss — index.py has self-healing" → bug
- "react-markdown is used for rendering project context, not a custom parser" → dependency

**Do NOT end a session without contributing at least one knowledge entry if you learned something new.**

<!-- sessionfs-knowledge-end -->
