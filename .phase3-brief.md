# Phase 3 — full surface sweep onto ui/ primitives (ticket tk_6553cbd7953d415e)

You are Prism. Same rules: branch `feat/dashboard-redesign` only, never push, sessionfsbot author, no AI mentions in commits.

This brief defines SIX chunks (A–F). **You execute ONLY the chunk named in the prompt that pointed you here.** Each chunk runs in a fresh session. Before starting, run `git log --oneline -8` — completed chunks are visible as `phase3(X):` commits; never redo a landed chunk.

## Shared rules for every chunk

- **Behavior-preserving migration.** You are restyling onto the design system, not redesigning flows. Zero functional changes (single exception: the chunk C board view).
- Use the `ui/` primitives (`dashboard/src/components/ui/`): Button, Card, Input/Textarea/Select, Dialog, Tabs, Table, Dropdown, Tooltip, Kbd. Replace bespoke buttons/cards/inputs/modals with primitives. Where a primitive lacks a needed prop, EXTEND the primitive (and its tests) rather than writing a one-off.
- Tokens only — no hardcoded hex outside index.css. Session/ticket/run IDs render as `.text-mono-chip`. Tool colors via `TOOL_COLORS` (`src/utils/tools.ts`).
- Group headers: 11px uppercase `text-micro text-tertiary`. Tables: Table primitive. Empty states: icon + 15px title + 13px tertiary hint + (where an obvious action exists) a Button.
- A11y preserved: focus traps on dialogs, ARIA labels, keyboard paths.
- Gate before committing: `npx tsc -b` clean AND `npm test` fully green. Commit message starts `phase3(<letter>):`.
- Finish with a summary: files changed, test delta, primitives extended, any deviations.

## Chunk A — Session surfaces
`src/sessions/SessionDetail.tsx`, `ConversationView.tsx`, `MessageBlock.tsx`, `ContentBlock.tsx`, `SummaryTab.tsx`, `AuditTab.tsx`, `AuditModal.tsx`.
Detail header: tool-colored edge + mono-chip ID (match the Phase 1b card language). Message blocks: user/assistant on `--surface`, tool results on `--bg-sunken`, thinking on `--bg-sunken` with tertiary text; role colors unchanged. Code blocks `--bg-sunken` + hairline border. AuditModal onto Dialog primitive.

## Chunk B — Project surfaces
`src/projects/ProjectsPage.tsx`, `ProjectDetail.tsx` (+ tab bar onto Tabs primitive), `RulesTab.tsx`, context/knowledge tabs.
Project cards onto Card (consider a subtle brand top-edge for org-scoped projects). Rules editor textarea onto Textarea; the max-tokens inputs onto Input. Markdown render surfaces on `--surface` with hairline borders.

## Chunk C — Tickets + kanban board + personas (THE feature chunk)
`src/projects/TicketsTab.tsx`, `PersonasTab.tsx`.
1. Migrate both tabs onto primitives (modals → Dialog, buttons → Button, etc.).
2. **NEW: board view for tickets.** Toggle (list/board icons, same segmented pattern as the sessions toggle) persisted as localStorage `sfs-tickets-view`, default `list`.
   - Columns derived from the statuses the tab already knows (its existing filter union). Empty columns render with a count-0 header, collapsed on mobile.
   - Cards: title, priority badge, persona mono-chip, kind icon (task/issue), comment count. Click = same expand/detail the list uses (or routes to it).
   - **State-mutation rule (critical):** the board adds NO new transitions. Buttons the list already has (approve, dismiss, comment) appear on cards in the relevant statuses. Drag-and-drop ONLY between status pairs whose transition already exists as a dashboard action — if none qualify, ship the board WITHOUT drag and say so in the summary. Lifecycle moves that are CLI/MCP-only (start/complete/resolve/escalate/block) must NOT become draggable.
3. Tests: toggle persistence, columns render with correct ticket placement, a CLI-only transition is NOT exposed via drag, existing list tests stay green.

## Chunk D — Coordination surfaces
`src/projects/AgentRunsTab.tsx`, handoffs pages/components, `TransferInbox.tsx`, `TransferPanel.tsx`, invites page.
Status chips onto Badge variants; run/handoff IDs as mono chips; timeline/event lists on the sunken ladder; accept/decline/cancel onto Button variants (danger for destructive).

## Chunk E — Account surfaces
`src/sessions/SettingsPage.tsx`, billing page, admin pages, `TrashView.tsx`, DLP settings.
Forms onto Input/Select; section cards onto Card; admin tables onto Table; destructive actions (purge, delete) onto Button danger + Dialog confirms.

## Chunk F — Auth + global polish
`src/auth/LoginPage.tsx` (+signup), getting-started, `src/help/HelpPage.tsx`.
Login: centered Card on canvas with the ambient gradient, brand-glow on the submit. Then a GLOBAL pass: every empty state styled per the shared rule; contrast audit both themes (fix any sub-AA text you find, in index.css if token-level); kill any straggler hardcoded hex you find anywhere in src/ (move to tokens).
