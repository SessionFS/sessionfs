{{- if .Values.license.key }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sessionfs.fullname" . }}-license
type: Opaque
stringData:
  license-key: {{ .Values.license.key | quote }}
{{- end }}
